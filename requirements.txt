requests>=2.31          # HTTP-запросы к публичным endpoint маркетплейсов
pandas>=2.0             # агрегация и группировки в анализе
pyyaml>=6.0             # чтение config.yaml
python-dateutil>=2.8    # разбор дат (зависимость pandas, фиксируем явно)
curl_cffi>=0.7          # маскировка под браузер Chrome: без неё WB отвечает 403
